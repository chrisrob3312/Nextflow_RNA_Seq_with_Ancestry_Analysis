/*
    LSF cluster profile
*/
process {
    executor = 'lsf'
    queue    = { task.memory >= 64.GB ? 'bigmem' : 'normal' }
}

executor {
    queueSize       = 100
    submitRateLimit = '10 sec'
    pollInterval    = '30 sec'
}
