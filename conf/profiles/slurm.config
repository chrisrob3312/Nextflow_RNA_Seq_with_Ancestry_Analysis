/*
    SLURM cluster profile
*/
process {
    executor = 'slurm'
    queue    = { task.memory >= 64.GB ? 'bigmem' : 'normal' }
    clusterOptions = '--account=cancer_rnaseq'
}

executor {
    queueSize       = 100
    submitRateLimit = '10 sec'
    pollInterval    = '30 sec'
}
